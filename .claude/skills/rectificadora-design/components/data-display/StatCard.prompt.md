One-line: Quick-stat metric card with icon chip, big number and caption.

```jsx
<StatCard icon={<Icon name="file" />} value="3.5K" label="Presupuestos activos" active />
```
Set `active` for the inverted black highlight (one card per row, like the reference).
