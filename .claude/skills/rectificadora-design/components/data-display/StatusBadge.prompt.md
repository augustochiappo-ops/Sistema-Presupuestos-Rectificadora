One-line: Status pill for tables/lists — Pending, Active, Expired.

```jsx
<StatusBadge status="active">Aprobado</StatusBadge>
```
`status`: `pending | active | expired`. Children override the label.
