One-line: List row with leading logo, title + timestamp, trailing badge.

```jsx
<ListItem leading={<img src="/apple.svg" />} title="Apple" meta="Jun 10 03:20 GMT" trailing={<StatusBadge status="pending" />} />
```
Used for panels like "Pending Approvals".
