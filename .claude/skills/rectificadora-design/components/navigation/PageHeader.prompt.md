One-line: Page title + subtitle with a right-aligned actions slot.

```jsx
<PageHeader title="Listado de Motores" subtitle="491 motores · todas las marcas"
  actions={<SearchInput placeholder="Buscar…" />} />
```
