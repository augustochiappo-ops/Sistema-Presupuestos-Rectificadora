One-line: Dropdown trigger button (label + chevron) for filters like "Status".

```jsx
<Select label="Status" open={menuOpen} onClick={toggle} />
```
Presentational only — render your own menu on click.
